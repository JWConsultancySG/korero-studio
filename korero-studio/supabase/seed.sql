-- Optional seed data (run in SQL Editor after `supabase/migrations/*`).
-- App state is stored in Postgres; see migrations for schema.

-- Example: insert a demo follow-up row (use a real profiles.id UUID and phone).
-- insert into public.korero_followups (student_id, student_phone, experience_level, note, payment_ref)
-- values ('<uuid-from-auth-profiles>', '6591234567', 'intermediate', 'Seed row from SQL', 'PAY-DEMO123');
