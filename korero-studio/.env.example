# Copy to .env.local in THIS folder (same as package.json). Save the file and restart `npm run dev`.
# https://supabase.com/dashboard/project/_/settings/api
NEXT_PUBLIC_SUPABASE_URL=your-project-url
# Use either the publishable key OR the legacy anon key (same variable role):
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-or-anon-key
# NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Email of the single shared staff account (must match a Supabase Auth user). Used to grant /admin access.
# Alternatively set app_metadata.korero_admin = true on that user in the Supabase Dashboard.
# NEXT_PUBLIC_ADMIN_EMAIL=studio@yourdomain.com
