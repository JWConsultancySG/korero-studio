import ForgotPasswordPage from "@/components/pages/ForgotPasswordPage";

/** Same flow as /auth/forgot-password — shorter URL for links in the app. */
export default function Page() {
  return <ForgotPasswordPage />;
}
